# Example: buickd on the host with upstreams on 127.0.0.1 (adjust ports to your apps).
# buick init --config ./buick.host.example.yml
certs:
  path: "./dev/buick/certs"
proxy:
  certs_path: "./dev/buick/certs"

services:
  api.localhost:
    target: "http://127.0.0.1:8080"
